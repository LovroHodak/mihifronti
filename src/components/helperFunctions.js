/* const products = [
    {id: 1, name: 'L-a', cat: 'a'},
    {id: 2, name: 'L-a2', cat: 'a'},
    {id: 3, name: 'L-a3', cat: 'a'},
    {id: 4, name: 'L-b', cat: 'b'},
    {id: 5, name: 'L-b2', cat: 'b'},
    {id: 6, name: 'L-c', cat: 'c'},
    {id: 7, name: 'L-d', cat: 'd'},
    {id: 8, name: 'L-e', cat: 'e'},
  ]

let sumarum = []

let sum = products.find((item) => {
  let answ = (item.cat === 'a')
  return answ
})
sumarum.push(sum)

let sum2 = products.find((item) => {
  return (item.cat === 'b')
})
sumarum.push(sum2)

let sum3 = products.find((item) => {
  return (item.cat === 'c')
})
sumarum.push(sum3)

let sum4 = products.find((item) => {
  return (item.cat === 'd')
})
sumarum.push(sum4)

let sum5 = products.find((item) => {
  return (item.cat === 'e')
})
sumarum.push(sum5)

console.log(sumarum) */
